/**
 * The data-analysis bundle is patch-only: its substance is the `insert` list
 * in `cordis.patch.yml`, which mounts the three host-plane plugins. This node
 * half has no runtime logic.
 * @module @andy1797833970/dsh-bundle-data-analysis
 */

/** Patch-only bundle; no host runtime to mount. */
export function apply(): void {}
